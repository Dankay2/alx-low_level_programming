#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @c: the int to check
 * Return: computes the absolute value of an integer n
 */
int _abs(int num)
{
	if (num < 0)
	{
		return (-num);
	}
	else
	{
	return (num);
	}
}
