#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @num: the int to check
 * Return: computes the absolute value of an integer num
 */
int _abs(int num)
{
	if (num < 0)
	{
		return (-num);
	}
	else
	{
	return (num);
	}
}
